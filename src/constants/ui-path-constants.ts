enum UI_PATH {
  HOME = "/",
  LOGIN = "/login",
  REGISTER = "/register",
  PROFILE = "/profile",
  BOOKING_HISTORY = "/booking-history",
  LOGOUT = "/logout",
  SEARCH = "/search",
  NOT_FOUND = "/404",
  ABOUT = "/about",
}

export default UI_PATH;
