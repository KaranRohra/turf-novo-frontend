enum UI_PATH {
  HOME = "/",
  LOGIN = "/login",
  SIGNUP = "/signup",
  PROFILE = "/profile",
  BOOKING_HISTORY = "/booking-history",
  LOGOUT = "/logout",
  SEARCH = "/search",
  NOT_FOUND = "/404",
  ABOUT = "/about",
}

export default UI_PATH;
